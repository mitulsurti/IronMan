[pytest]
pythonpath = .
testpaths = tests
addopts = -q -m "not live"
markers =
	live_azure: optional live Azure provider smoke test; skipped without configuration
	live_yahoo: optional live Yahoo Finance provider validation; excluded from the normal suite
	live: explicitly opt-in external connectivity validation; excluded from the normal suite
